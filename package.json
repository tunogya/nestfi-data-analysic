{
  "name": "nestfiasync",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "asyncBlock": "pm2 start src/asyncBlock.js --name initAsyncBlock",
    "cronAsyncBlock": "pm2 start src/asyncBlock.js --name cronAsyncBlock --cron '*/10 * * * *'",
    "cronClearingFuture": "pm2 start src/clearingFuture.js --name clearing --cron '0 1 * * *'",
    "cronSettlement": "pm2 start src/settlement.js --name settlement --cron '0 2 * * *'"
  },
  "dependencies": {
    "@ethersproject/bignumber": "^5.7.0",
    "dotenv": "^16.0.3",
    "knex": "^2.4.2",
    "mysql": "^2.18.1",
    "node-fetch": "^3.3.1",
    "pm2": "^5.3.0"
  }
}
